class User {
    constructor(id, name, contact, addresses, orders) {
        this.id = id;
        this.name = name;
        this.contact = contact;
        this.addresses = addresses;
        this.orders = orders;
    }
}